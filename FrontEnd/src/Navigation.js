import React from 'react';

const Navigation = () => {
    return (
        <nav>
            <a href="/">Home</a>
            <a href="/login">Login</a>
            <a href="/signup">Sign Up</a>
        </nav>
    );
}

export default Navigation;
